package com.example.skylink.viewmodel.clickListeners

//Interfaz usada para manejar el click de items en el recycler view de precios
interface OnPriceClickListener {
    fun onItemClick(titulo: String)
}