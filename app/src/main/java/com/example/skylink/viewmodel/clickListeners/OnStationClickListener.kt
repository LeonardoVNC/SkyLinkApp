package com.example.skylink.viewmodel.clickListeners

//Interfaz usada para manejar el click de items en los recycler views de las estaciones
interface OnStationClickListener {
    fun onItemClick(input: Int)
}